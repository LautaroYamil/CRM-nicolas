NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Secret key (Settings > API > Secret keys). Server-only, nunca exponer al cliente.
# Necesaria para /admin/users (invitar vendedores).
SUPABASE_SERVICE_ROLE_KEY=
